import { SettingsRepository } from './settings-repository';

describe('SettingsRepository', () => {
  it('should create an instance', () => {
    expect(new SettingsRepository()).toBeTruthy();
  });
});
